#!/usr/bin/env bash
restartmebaby () { echo "sleeping for 5 minutes then will try again..."; sleep 300 && aptitude update -y || restartmebaby; }
echo "changing password for root..."
passwd root
echo "changing password for kali..."
passwd kali
echo "installing packages"
apt-get install -y aptitude
aptitude update -y || restartmebaby
aptitude upgrade -y
aptitude install -y redshift
redshift -O 1337
aptitude install -y fonts-noto-color-emoji
